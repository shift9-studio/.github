# Flat node_modules so the workspace packages (@shift9/ui, @shift9/motion)
# resolve a single shared copy of react / @types/react / framer-motion at
# both type-check and bundle time. Avoids duplicate-React hook errors and
# the "empty HTMLAttributes" type-resolution issue under strict isolation.
node-linker=hoisted
