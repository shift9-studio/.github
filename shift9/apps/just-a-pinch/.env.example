# ─────────────────────────────────────────────────────────────────────
# Just a Pinch — public content (Supabase)
#
# These are PUBLIC, read-only keys. They ship in the browser bundle and are
# safe to commit / expose: row-level security governs all access, and the
# only table this surface reads is `featured_recipes` (public SELECT policy).
# The service-role key is NOT used here and must never live in this app.
#
# Local dev:  cp .env.example .env.local
# Production: set the same two vars in the Vercel project settings.
# ─────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=https://qdlfiewspjgbucszezja.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_b9eEZVkYfxnyBwHQJP6DjA_9nKu0QoL
