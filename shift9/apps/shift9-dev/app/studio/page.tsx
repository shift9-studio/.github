import { StudioDolly } from "../_components/StudioDolly";

export const metadata = {
  title: "Shift-9 — The Studio",
  description:
    "Twelve projects as one continuous take. Live work resolved and settled, work in development raw and unstable.",
};

export default function StudioPage() {
  return <StudioDolly />;
}
