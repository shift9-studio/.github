packages:
  - "apps/*"
  - "packages/*"
# Security patches for pinned transitive dependencies; remove when upstream ranges catch up.
# Checked by scripts/check-dependency-security.mjs and both application builds.
overrides:
  'postcss@<8.5.23': 8.5.23
  'sharp@0.34.5': 0.35.4
