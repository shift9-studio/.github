# Room Explore hero assets stay as regular git blobs so Vercel/preview
# always gets the real HDRI and GLBs (LFS pointers were shipping as 132-byte files).
shift9/apps/shift9-dev/public/experience/room/**/*.glb -text -filter -diff -merge
shift9/apps/shift9-dev/public/experience/room/**/*.hdr -text -filter -diff -merge
